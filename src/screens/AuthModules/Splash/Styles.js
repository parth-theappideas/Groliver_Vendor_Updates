import { StyleSheet } from 'react-native'
import { vs, hs, fs, screenWidth, screenHeight } from '../../../utils/stylesUtils'

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    splashImage: {
        width: '100%',
        height: '100%'
    }
})